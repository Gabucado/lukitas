[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gabucado", email = "awsalinas@uc.cl" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
app = "app:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
